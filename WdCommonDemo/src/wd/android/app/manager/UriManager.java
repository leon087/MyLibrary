package wd.android.app.manager;

import java.util.Map;

import android.content.Context;

public class UriManager {

	/**
	 * 根据类型跳转到不同界面
	 * 
	 * @param <T>
	 * @param context
	 * @param map
	 */
	public static <T> void gotoPage(Context context, Map<String, T> map) {
		// int type = MapUtil.getInt(map, Tag.LINK_TYPE);
	}
}
